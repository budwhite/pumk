<div class='hero-unit'>
  <h1>Welcome to PickUpMyKid!</h1>
  <p class='lead'>PickUpMyKid is a carpool matching utility that matches you with other parents who are taking the same daily drop-off and pick-up trip.</p>
  <ul>
    <li>
    <span class='lead'>
      Driving your kid to and from school? make money when you offer to pick up another kid.
    </span>
    </li>
    <li>
    <span class='lead'>
      Busy parent need help with kids school commute? find another parent who's making the same trip to help out.
    </span>
    </li>
  </ul>
</div>

<div class='row-fluid'>

  <div class='span6'>

    <%= simple_form_for resource, :as => resource_name, :url => session_path(resource_name) do |f| %>
      <legend>Sign in</legend>
      <%= devise_error_messages! %>
      <%= f.input :email %>
      <%= f.input :password %>
      <% if devise_mapping.rememberable? -%>
        <%= f.input :remember_me, as: :boolean %>
      <% end -%>
      <%= f.button :submit, 'Sign in', class: 'btn-success' %>
    <% end %>

  </div>

  <div class='span6'>

    <%= simple_form_for resource, as: resource_name, url: registration_path(resource_name), html: { class: 'form-horizontal' } do |f| %>
      <legend>Sign up</legend>
      <%= devise_error_messages! %>
      <%= f.input :first_name %>
      <%= f.input :last_name %>
      <%= f.input :email %>
      <%= f.input :password %>
      <%= f.input :password_confirmation %>
      <%= f.input :verified, as: :hidden, input_html: { checked: false } %>
      <%= f.button :submit, 'Sign up', class: 'btn-success' %>
    <% end %>
  </div>
</div>

<% if false %>
<div class='row-fluid'>
  <div class='span12 text-center'>
    <h2>How It Works</h2>
  </div>
</div>

<br></br>

<div class='row-fluid'>
  <div class='span4'>
    <div class='text-center'><i class="icon-home icon-4x" style='color:blue'></i></div>
    <br></br>
    <h4 class='text-center'>Find other Cherry Crest families who live close to you</h4>
  </div>
  <div class='span4'>
    <div class='text-center'><i class="icon-car-sign icon-4x" style='color:blue'></i></div>
    <br></br>
    <h4 class='text-center'>Form carpools in which either you are the parent driver or your kid is the little rider</h4>
  </div>
  <div class='span4'>
    <div class='text-center'><i class="icon-calendar icon-4x" style='color:blue'></i></div>
    <br></br>
    <h4 class='text-center'>We'll take care of scheduling, reminders via emails or text and handling of payment</h4>
  </div>
  
</div>
<% end %>
